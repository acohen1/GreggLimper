[pytest]

# Suppress deprecation warnings from discord.py voice player
# (the library relies on audioop, which is deprecated on Python 3.13)
filterwarnings =
    ignore::DeprecationWarning:discord.player
    ignore:'audioop' is deprecated:DeprecationWarning
